{
  "name": "chords4all",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "svelte-kit sync",
    "predev": "node scripts/ensure-deps.mjs",
    "prebuild": "node scripts/ensure-deps.mjs",
    "precheck": "node scripts/ensure-deps.mjs",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-vercel": "^5.9.0",
    "@sveltejs/kit": "^2.55.0",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@types/node": "^22.19.17",
    "svelte": "^5.0.0",
    "svelte-check": "^4.4.6",
    "typescript": "^5.9.3",
    "vite": "^8.0.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
